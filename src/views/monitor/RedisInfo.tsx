import { defineComponent } from "vue";

export default defineComponent({
    name: 'RedisInfo',
    setup() {

        return () => {
            return (
                <div>
                    Redis信息在那里？
                </div>
            )
        }
    }
})